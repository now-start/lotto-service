spring:
  application:
    name: lotto

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    process:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

eureka:
  client:
    service-url:
      defaultZone: ${SERVICE_URL:https://spring.nowstart.org/admin/eureka/}

logging:
  file:
    path: ./log
  charset:
    file: UTF-8

lotto:
  id: ${LOTTO_ID:phantom2691}
  password: ${LOTTO_PASSWORD:@tjdgh2691}
  email: ${LOTTO_EMAIL:phantom2691@naver.com}
  count: ${LOTTO_COUNT:5}
  cron:
    check: ${LOTTO_CRON_CHECK:0 0 22 * * 6}
    buy: ${LOTTO_CRON_BUY:0 0 9 * * 0}